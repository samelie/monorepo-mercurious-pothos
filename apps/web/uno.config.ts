import config from '@you/styles/uno.config'
import { mergeDeep } from 'unocss'

// Unocss({
//   presets: [
//   ],
// })
// import type { Theme } from 'unocss/preset-uno'

export default mergeDeep(config, {})
